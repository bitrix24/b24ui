---
title: CSS Variables
description: Bitrix24 UI uses CSS variables as design tokens for flexible, consistent theming with built-in light and dark mode support.
---

## Colors

::warning
We are still updating this page. Some data may be missing here — we will complete it shortly.
::

## Text

Bitrix24 UI provides Tailwind CSS utility classes for text colors, allowing you to use class names like `text-legend` or `text-description`:

::code-preview{class="[&>div>p]:flex [&>div>p]:flex-wrap [&>div>p]:gap-2"}
[Dimmed]{class="text-dimmed text-sm px-4 py-1.5 inline-block rounded-md"}
[Muted]{class="text-muted text-sm px-4 py-1.5 inline-block rounded-md"}
[Description]{class="text-description text-sm px-4 py-1.5 inline-block rounded-md"}
[Legend]{class="text-legend text-sm px-4 py-1.5 inline-block rounded-md"}
[Label]{class="text-label text-sm px-4 py-1.5 inline-block rounded-md"}

#code
```vue
<template>
  <span class="text-dimmed">Dimmed</span>
  <span class="text-muted">Muted</span>
  <span class="text-description">Description</span>
  <span class="text-legend">Legend</span>
  <span class="text-label">Label</span>
</template>
```
::

## Background

Bitrix24 UI provides Tailwind CSS utility classes for background colors, allowing you to use class names like `bg-default` or `bg-muted`:

::code-preview{class="[&>div>p]:flex [&>div>p]:flex-wrap [&>div>p]:gap-2"}
[Default]{class="bg-default text-sm px-4 py-1.5 inline-block rounded-md"}
[Muted]{class="bg-muted text-sm px-4 py-1.5 inline-block rounded-md"}
[Elevated]{class="bg-elevated text-sm px-4 py-1.5 inline-block rounded-md"}
[Accented]{class="bg-accented text-dimmed text-sm px-4 py-1.5 inline-block rounded-md"}

#code
```vue
<template>
  <div class="bg-default">Default</div>
  <div class="bg-muted">Muted</div>
  <div class="bg-elevated">Elevated</div>
  <div class="bg-accented text-dimmed">Accented</div>
</template>
```
::

## Border

Bitrix24 UI provides Tailwind CSS utility classes for border colors, allowing you to use class names like `border-default` or `border-muted`:

::code-preview{class="[&>div>p]:flex [&>div>p]:flex-wrap [&>div>p]:gap-2"}
[Default]{class="border-2 border-default text-sm px-4 py-1.5 inline-block rounded-md"}
[Muted]{class="border-2 border-muted text-sm px-4 py-1.5 inline-block rounded-md"}
[Accented]{class="border-2 border-accented text-sm px-4 py-1.5 inline-block rounded-md"}

#code
```vue
<template>
  <div class="border border-default">Default</div>
  <div class="border border-muted">Muted</div>
  <div class="border border-accented">Accented</div>
</template>
```
::

## Radius

::warning
We are still updating this page. Some data may be missing here — we will complete it shortly.
::

## Container

Bitrix24 UI provides a `--b24ui-container-width` CSS variable that controls the maximum width of the [Container](/docs/components/container/) component.

```css
:root {
  --b24ui-container-width: 90rem; /* 1440px */
}
```

## Header

Bitrix24 UI provides a `--b24ui-header-height` CSS variable that controls the height of the [Header](/docs/components/header/) component.

```css
:root {
  --b24ui-header-height: 3.625rem;
}
```

::tip
You can customize this value in your `main.css` to adjust header height consistently throughout your application:

```css [app/assets/css/main.css]
@import "tailwindcss";
@import "@bitrix24/b24ui-nuxt";

:root {
  --b24ui-container-width: 90rem;
}
```
::

## Body

Bitrix24 UI applies default classes on the `<body>`{lang="html"} element of your app for consistent theming across light, edge-light, edge-dark and dark modes:

```css
body {
  scrollbar-gutter: stable;
  background: var(--air-theme-background);
  @apply antialiased font-(family-name:--ui-font-family-system) text-legend scheme-light dark:scheme-dark edge-light:scheme-light edge-dark:scheme-light
}
```
