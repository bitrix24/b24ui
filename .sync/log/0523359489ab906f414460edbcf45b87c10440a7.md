# port — nuxt/ui@0523359489ab906f414460edbcf45b87c10440a7

**Upstream:** chore(deps): update all non-major dependencies (#6468)

**Decision:** port (full apply — all 4 bumped deps shared with b24ui).

## Change
Upstream bumped 4 deps in `docs/package.json` (+ lockfile). b24ui's
`docs/package.json` carried all 4 at the identical pre-versions, so applied the
same ranges and regenerated `pnpm-lock.yaml`:
- `@nuxtjs/mcp-toolkit` ^0.16.1 → **^0.17.1**
- `@takumi-rs/core` ^1.2.1 → **^1.3.0**
- `nuxt-og-image` ^6.5.0 → **^6.5.1**
- `shiki` ^4.0.2 → **^4.1.0**

`package.json` ranges match upstream exactly; the lockfile is b24ui's own
fresh regen (`pnpm install`), so resolved versions are latest-in-range at
install time (e.g. takumi 1.8.1, og-image 6.6.0, shiki 4.2.0) — expected for a
diverged dependency tree, not pinned to upstream's lock.

## Notes
- Root `package.json` untouched (upstream only changed `docs/`).
- Peer warning: `@nuxtjs/mcp-toolkit@0.17.2` wants `vue@^3.5.34`, workspace
  resolves `vue@3.5.32` — docs-only tooling, non-fatal; typecheck + docs
  compilation pass.

## Validation
- ✅ dev:prepare · ✅ typecheck (incl. `nuxt typecheck docs`) · ✅ lint ·
  ✅ vitest run (4972 passed | 6 skipped) · ✅ module build.
- ✅ `docs:build` **compiles** (client + server built OK). The nitro
  **prerender** step fails locally with `EMFILE: too many open files` — a
  sandbox file-descriptor cap (4096, not raisable here); it errors on
  `@nuxtjs/mdc@0.22.0` (a dep NOT bumped here), confirming it's environmental,
  not a regression. CI runs docs build under normal limits.
