# Port: chore(deps): update all non-major dependencies

**Upstream:** `6bcc97a62161ffe0c13c8595b5d7f97d209f2d54` (nuxt/ui, #6849)
**Decision:** port — 23 of 24 bumps; one blocked and held, with evidence

## Upstream change

A Renovate batch across `package.json`, `docs/package.json` and the two
playground manifests, plus the lockfile.

## b24ui port

Ported at the maintainer's instruction to **align to upstream's versions
outright**, rather than following only the deltas. That matters here because
this fork had drifted behind on several packages, so what upstream shipped as a
non-major batch includes two **major** jumps for us:

| package | upstream | ours before | now |
|---|---|---|---|
| `ai` | `^6.0.214` → `^7.0.66` | `^6.0.214` | `^7.0.66` |
| `@ai-sdk/vue` | `^3.0.214` → `^4.0.66` | `^3.0.214` | `^4.0.66` |

The rest are patch/minor: `@unhead/vue`, `magic-string`, `motion-v`, `ohash`,
`vue-component-type-helpers`, `vue-tsc`, `@nuxtjs/mcp-toolkit`, `@regle/core`,
`@regle/rules`, `@shikijs/types`, `shiki`, `vue-component-meta`.

Per §2, dependency manifests mirror across **all** b24ui playgrounds, not the
two upstream has. `playgrounds/demo` carries `ai`, `@ai-sdk/vue` and `vue-tsc`
and was bumped with the others — its `vue-tsc` was further behind than the rest
(`^3.3.7`), which only a per-playground pass would have caught. `playgrounds/repl`
carries none of them.

## `nuxt-schema-org` is held at `^6.2.1`, deliberately

The one bump not taken. It is not a judgement call — the newer versions do not
run here:

```
[error] (0 , _kit3.useModuleLogger) is not a function
    at setup (nuxt-schema-org@6.2.9/dist/module.mjs:98:46)
```

The cause is transitive rather than direct. Every published `nuxt-schema-org`
imports only symbols `@nuxt/kit@4.5.2` actually exports — checked all of
`6.2.1`…`6.2.9` against the 98 names our kit exports, none missing. What breaks
is `nuxt-site-config`, which any bump above `6.2.1` drags from `4.0.8` to
`4.2.3`, and 4.2.3 calls `useModuleLogger` and `setupNitroRuntimeCompatibility`.
Neither exists in `@nuxt/kit@4.5.2`, and 4.5.2 is the newest published 4.x —
so there is no version of Nuxt we could move to that would fix it.

Pinning `6.2.7` was tried first and fails the same way, one function earlier
(`setupNitroRuntimeCompatibility`), which is what identified `nuxt-site-config`
as the real constraint rather than `nuxt-schema-org` itself.

**Upstream has the same defect and does not see it.** Their lockfile at this
commit resolves `nuxt-schema-org@6.2.9`, and their `@nuxt/kit` is `^4.5.2` like
ours. It stays latent because neither CI builds the docs site; here it surfaced
in `dev:prepare`, which prepares `docs/` as well.

Restoring the caret alone was not enough — `^6.2.1` is satisfied by `6.2.7`, so
pnpm kept the newer resolution. The lockfile had to be restored from `HEAD`
before reinstalling, after which the manifest entry is byte-identical to before
and the resolution is back to `6.2.1` / `nuxt-site-config@4.0.8`.

## Verify (CI=true)

`dev:prepare` (all four apps) · `lint` · `typecheck` · `test` (6546 passed, 6
skipped, 288 files) · `build` · `docs:generate` (1240 routes) — all green.

`typecheck` is the load-bearing one for the two majors: it runs `nuxt typecheck`
over `docs/`, and every `ai` / `@ai-sdk/vue` call site lives there —
`docs/server/api/{ai,chat,completion}.post.ts`, `docs/modules/bx-assistant/*`
and the `chat/` and `editor/` examples.

**Caveat worth stating.** The AI features compile and prerender, but their
runtime paths were not exercised: `docs:generate` runs with
`NUXT_PUBLIC_USE_AI=false` and no `DEEPSEEK_API_KEY`, so the assistant is
disabled during the build. A v7 behavioural change inside a request handler
would not show up in any check run here.
