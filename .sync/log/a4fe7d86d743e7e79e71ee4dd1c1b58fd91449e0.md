# No-op: docs(page-hero): resize image in usage example

**Upstream:** `a4fe7d86d743e7e79e71ee4dd1c1b58fd91449e0` (nuxt/ui)
**Decision:** no-op — nothing here for it to apply to

## Upstream change

One line in `docs/content/docs/2.components/page-hero.md`: the usage example's
screenshot was declared at its intrinsic size and is now declared at display
size.

```diff
-![App screenshot](/blocks/image4.png){width="4804" height="3000" …}
+![App screenshot](/blocks/image4.png){width="960" height="540" …}
```

## b24ui

This fork has no `PageHero` component and no `page-hero.md` — it ships its own
`Page*` family instead. Checked rather than inferred from the filename:

- no `src/runtime/components/PageHero.vue`
- no match for `page-hero`/`PageHero` anywhere in `docs/content` or `docs/app`
- no reference to `/blocks/image4.png` anywhere in `docs/`

Same call as `0f2b9da0` (`docs(page-hero): wrong mdc formatting`), which the
ledger already records as a no-op for the same reason. Recorded rather than
skipped silently so the next porter does not re-derive it a third time.
