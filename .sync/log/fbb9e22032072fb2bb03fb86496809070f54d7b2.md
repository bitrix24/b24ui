# n/a — nuxt/ui@fbb9e22032072fb2bb03fb86496809070f54d7b2

**Upstream:** fix(ProseCodeTree): invoke default slot from render (#6906)

**Decision:** n/a — the component does not exist in this fork.

Second of two contiguous commits taken in one PR (§6 4b), with `c4ee0ea3`.

## Upstream change

Four files, all of them `CodeTree`:

- `src/runtime/components/prose/CodeTree.vue` — the fix itself;
- `test/components/prose/CodeTree.spec.ts` — 169 new lines covering it;
- `playgrounds/nuxt/app/pages/components/typography.vue` — a new 391-line page,
  added as the surface on which to exercise the fix by hand;
- `playgrounds/nuxt/app/composables/useNavigation.ts` — one line registering it.

## Why nothing applies

`src/runtime/components/prose/` here holds 43 components and **`CodeTree` is not
among them** — checked by listing the directory rather than by grepping for the
name, so a rename would have shown up too. Nothing in `src/theme/` references it
either. The fix has no target, its test has nothing to mount, and the playground
page is built around upstream's prose set including the component we lack.

## Recorded, not acted on

The new playground page is worth noting separately from this decision, because
the observation survives it: this fork has **43 prose components and no
playground page for any of them**, while upstream now has one. That is a gap of
ours rather than a port — building it would mean writing our own page for our own
components, not carrying upstream's across — so it is written down here and left.

Same shape as the InputMenu playground row found while backfilling `2799fa6f`:
recorded in the log first, fixed later on a maintainer's word (#520).
