# Port: fix(SelectMenu): bind `id` and aria attributes on trigger (#6572)

**Upstream:** `c3bef7a21bdd67343492f01e874044e59894b95b` (nuxt/ui)
**Decision:** port

## Upstream change
`SelectMenu.vue`: move `id`, `$attrs` and `ariaAttrs` off the `ComboboxRoot`
onto the actual `ComboboxTrigger` (so the id/aria land on the focusable
element). `ComboboxRoot` v-bind narrows from
`({ ...rootProps, ...$attrs, ...ariaAttrs } as any)` to `(rootProps as any)`.

## b24ui adaptation
Same move, b24ui-namespaced:
- `ComboboxRoot`: drop `:id="id"`; v-bind `{ ...rootProps as any, ...$attrs, ...ariaAttrs }`
  → `(rootProps as any)`.
- `ComboboxTrigger`: add `:id="id"` and `v-bind="{ ...$attrs, ...ariaAttrs }"`.
- regenerated SelectMenu snapshots (10 updated — the `id`/aria attrs move from
  the root element to the trigger).

## Verification (pnpm 11.5.2, gate ON)
dev:prepare · lint · typecheck · test (5086 passed, 6 skipped) · module build —
all green.

Platform note: b24ui CI is `ubuntu-latest` only.
