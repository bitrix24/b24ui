# port — nuxt/ui@631f5dc543fecaa36d5090a10a3db3223e9671b5

**Upstream:** fix(ContentSearch/DashboardSearch): proxy missing CommandPalette props (#6505)

**Decision:** port (#70, `e92fdc35`).

> **Backfilled 2026-08-30.** Never written at the time; the convention starts at
> `d50c121c`, one entry earlier in the ledger. Reconstructed from `git show` on
> both commits and from `e92fdc35`'s message. Re-derived, not recalled.

## Upstream change

`DashboardSearch` and `ContentSearch` both wrap `CommandPalette` and forward a
hand-written subset of its props. The subset had fallen behind the component:
eleven props existed on `CommandPalette` and reached neither wrapper. Two lines
per file, four in total — the `Pick<CommandPaletteProps<…>, …>` in the props
interface, and the `reactivePick` that actually forwards them:

```diff
-const commandPaletteProps = useForwardProps(reactivePick(props, 'size', 'icon', 'placeholder', 'autofocus', 'loading', 'loadingIcon', 'close', 'closeIcon', 'searchDelay'))
+const commandPaletteProps = useForwardProps(reactivePick(props, 'size', 'icon', 'trailingIcon', 'selectedIcon', 'childrenIcon', 'placeholder', 'autofocus', 'loading', 'loadingIcon', 'close', 'closeIcon', 'back', 'backIcon', 'disabled', 'highlightOnHover', 'labelKey', 'descriptionKey', 'preserveGroupOrder', 'virtualize', 'searchDelay'))
```

Added: `trailingIcon`, `selectedIcon`, `childrenIcon`, `back`, `backIcon`,
`disabled`, `highlightOnHover`, `labelKey`, `descriptionKey`,
`preserveGroupOrder`, `virtualize`.

## b24ui port

Same two files, same four lines, same eleven names in the same order — with one
deliberate subtraction. Upstream's lists contain `loadingIcon`; ours do not,
because **our pre-image did not contain it either**. Both fork files already read
`'…', 'loading', 'closeIcon', …` where upstream reads `'…', 'loading',
'loadingIcon', 'closeIcon', …`, so `loadingIcon` was not a prop this commit
failed to forward — it is a prop this fork does not carry. `CommandPalette.vue`
says so at its declaration site: `@memo not use loadingIcon`.

That distinction is the whole content of the port decision, and `e92fdc35`
recorded it correctly ("`loadingIcon` intentionally left out (pre-existing b24ui
divergence)"). Taking upstream's list verbatim would have added a name to a
`reactivePick` for a prop that does not exist here — silently forwarding
`undefined` rather than failing, which is the kind of divergence that is only
found by reading.

Nothing else diverges: no `ui`/`b24ui` rewrite is needed in these two lines, no
icon-component substitution, no test or snapshot movement — upstream shipped none
either, since forwarding a prop nobody sets changes no render.

## Verification (2026-08-30)

Checked against `main` rather than assumed: `DashboardSearch.vue:116` and
`content/ContentSearch.vue:176` both carry the full forwarded list with
`loadingIcon` still absent, and `CommandPalette.vue:93` still carries the
`@memo not use loadingIcon` that justifies it. Intact.
