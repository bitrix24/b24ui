# n/a — nuxt/ui@ff573acfa84e487b07dea71eda5a6506d5c2937a

**Upstream:** chore(skills): use create nuxt for templates and note AI SDK v7

**Decision:** n/a — neither hunk has a counterpart.

- **`guidelines/conventions.md`** swaps ten `npx nuxi@latest init -t ui/<name>`
  lines for `npm create nuxt@latest -- -t ui/<name>`. Those are nuxt's template
  registry entries for Nuxt UI. The fork's "Official templates" section exists,
  but was rewritten around this fork's own repositories — `git clone
  https://github.com/bitrix24/templates-dashboard.git` and
  `https://github.com/bitrix24/starter-b24ui.git` — and has no `nuxi` line to
  change.
- **`layouts/chat.md`** gains a note that its examples target AI SDK v7. This
  fork's `skills/b24-ui-nuxt/references/layouts/` holds `dashboard.md` and
  `landing.md` only.
