# port — nuxt/ui@081cf01acb50041aa67c339350076eb7c816b10d

**Upstream:** chore(deps): update all non-major dependencies (#6458) — renovate

**Decision:** port (selective — only deps b24ui shares; not 1:1).

**What:** adopted upstream's non-major version bumps for every dependency that
b24ui actually has, at the same pre-bump version:
- **root** `package.json`: `ai` 6.0.185, `eslint` 10.4.0, `vitest` 4.1.6.
- **docs**: `@ai-sdk/mcp` 1.0.42, `@ai-sdk/vue` 3.0.185, `@nuxt/content` 3.14.0,
  `@nuxtjs/mdc` 0.22.0, `@regle/core`/`@regle/rules` 1.25.2, `@takumi-rs/core`
  1.2.1, `ai` 6.0.185, `better-sqlite3` 12.10.0, `fflate` 0.8.3,
  `vue-component-meta` 3.3.0.
- **playgrounds/nuxt** + **playgrounds/demo** (mirror): `@ai-sdk/vue` 3.0.185,
  `@nuxt/content` 3.14.0, `ai` 6.0.185.

**Skipped (absent in b24ui):** `@ai-sdk/anthropic`, `@ai-sdk/gateway`,
`@iconify-json/{lucide,simple-icons,vscode-icons}` — b24ui uses
`@bitrix24/b24icons` + `@ai-sdk/deepseek`, so these upstream bumps don't apply.

**b24ui-specific resolution step:** after `pnpm install`, the new tree pulled in
two `jiti` versions (2.6.1 + 2.7.0), producing two `vite@7.3.2` type instances
→ `typecheck` failed with "Two different types with this name exist". Ran
`pnpm dedupe` to collapse `jiti` to a single 2.7.0; typecheck then green.
The `pnpm-lock.yaml` includes both the bump and the dedupe.

**Validation:** `pnpm install` + `pnpm dedupe` · `dev:prepare` · full
`typecheck` (green after dedupe) · `lint` · full `vitest run`
(4950 passed | 6 skipped) · module `build`.
