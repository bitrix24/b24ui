# n/a — nuxt/ui@69ace4c92a89afbec6893825659db5805e610ceb

**Upstream:** docs(showcase): add neleto.io (#7010)

**Decision:** n/a — two lines in `docs/content/showcase.yml` plus a 2.6 MB
screenshot, adding a site built with `@nuxt/ui` to upstream's showcase.

`showcase.yml` exists here, but its entries are sites built with *this* library.
Taking the entry would publish a claim that neleto.io uses Bitrix24 UI, which is
false. Same reasoning, and the same decision, as the two most recent showcase
commits in this ledger: `d41d3d33` (Ace and whichcoding.tools) and `26a19da4`
(fontnode.com). Earlier showcase commits were recorded as `no-op`; the ledger
moved to `n/a` for this class and this entry follows the later practice.
