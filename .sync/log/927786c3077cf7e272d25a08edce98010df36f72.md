# n/a — nuxt/ui@927786c3077cf7e272d25a08edce98010df36f72

**Upstream:** chore(github): disable `tia` dry run

**Decision:** n/a — third in the run that starts at `80932f85`. It edits
`.github/tia.yml`, the config for a triage bot installed on the `nuxt`
organisation:

```diff
-# nuxi triage config. Reference: https://github.com/benjamincanac/nuxi#configuration
-dryRun: true
+# https://github.com/benjamincanac/tia#configuration
```

Taking the bot out of dry run is a decision about how issues are triaged in
`nuxt/ui`. This fork has no bot config at all — see `80932f85` for the full
reasoning — so there is nothing here to take out of dry run.
