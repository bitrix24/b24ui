# port — nuxt/ui@beb7d4645e6507204b0bb27e5d63b2c81c764c24

**Upstream:** chore(skills): reference props and methods that exist in v4 (#6937)

**Decision:** port, one of four corrections. The other three are already right
here, or were never wrong. Second of two commits in this run.

## Upstream change

Four factual corrections to their agent skill — claims about props and methods
that do not exist. Worth taking seriously rather than filing as "docs": a skill
is read by an agent that will write the wrong call and find out at runtime.

Checked one by one against **our** skill and **our** source, not against
upstream's description.

### 1. `mode="drawer"` on Modal/Slideover — already corrected here, and better

Upstream replaces *"Modal and Slideover support `mode="drawer"`"* with a line
pointing at `UDashboardSidebar`. Our `component-selection.md:19` already reads:

> There is no `mode="drawer"` on Modal or Slideover — for drawer-like behaviour
> give `B24Slideover` `side="bottom"`, or use `B24Drawer` directly

Same correction, and it goes further: it says what to do instead for the reader
who wanted a drawer-shaped overlay, rather than redirecting to a sidebar
component that answers a different question.

### 2. `CommandPalette` and nested arrays — already corrected here, and earlier

Upstream splits its "components supporting nested arrays" list and adds a
sentence about `groups`. Our `conventions.md:116` already excludes it and says:

> `B24CommandPalette` groups the same content differently — it has no top-level
> `items` prop at all, only `groups`, each with its own flat `items`.

### 3. `validate()` and `clearErrors()` — half applies, and the half that does is a live trap

`clearErrors()` → `clear()` was already right here (`forms.md:283`).

The `validate()` half was **not**. Our example was:

```ts
const result = await form.value?.validate()
if (result) { /* valid — submit */ }
```

Verified against our own `Form.vue` rather than upstream's wording:
line 247 reads `if (opts.silent) return false`, and line 248 `throw new
FormValidationException(...)`. So without `silent` it throws, and `if (result)`
is unreachable on failure — the example teaches a shape that cannot work.

Both calls now pass `{ silent: true }`, and a paragraph says why, naming the
source lines rather than asserting the behaviour.

### 4. `ChatPrompt` accepts all Textarea props — n/a, we never claimed it

Upstream's skill said *"Accepts all Textarea props + `error` prop"*; they replace
it with a description of the real subset. The claim is true of our component too
— `ChatPrompt.vue:10` is `extends Pick<TextareaProps, 'rows' | 'autofocus' | …>`,
a selected subset, and `submitOnEnter` is ours as well.

But our skill never made the claim: `components.md:201` is a one-line table entry
reading *"Enhanced textarea for prompts"*, and we have no `layouts/chat.md`. There
is nothing to correct.

## Verification

`test/utils/skill-manifest.spec.ts` passes, 30 assertions.
