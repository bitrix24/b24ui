# Port: chore(deps): update nuxt framework to ^4.4.7 (#6549)

**Upstream:** `5c8343041c8ed92f7f10c4bc3eb0ed111e8e7aaf` (nuxt/ui)
**Decision:** port

## Upstream change
- Bump `nuxt`, `@nuxt/kit`, `@nuxt/schema` `^4.4.6` → `^4.4.7` (root + docs +
  playgrounds/nuxt), `@nuxt/kit` override in `pnpm-workspace.yaml`, add `h3:
  1.15.11` override, lockfile regen.
- `docs/nuxt.config.ts`: drop the `a11y: { logIssues: false }` block.
- `LinkBase.vue`: `href?: string` → `href?: string | null`.

## b24ui adaptation
Version bumps applied 1:1 where b24ui matches; the non-dep parts were already
satisfied:
- `package.json`: `@nuxt/kit`/`@nuxt/schema`/`nuxt` `^4.4.6` → `^4.4.7`.
- `docs/package.json`, `playgrounds/nuxt`, `playgrounds/demo` (demo mirrored
  per the playground invariant): `nuxt` → `^4.4.7`. repl/vue don't pin nuxt.
- `pnpm-workspace.yaml`: `@nuxt/kit` override `^4.4.6` → `^4.4.7`.
- lockfile regen under the gate (resolved nuxt was already `4.4.8`, within
  range — only specifier metadata changed).

**No-ops (already in b24ui state):**
- `h3: 1.15.11` override — already present in `pnpm-workspace.yaml`.
- `docs/nuxt.config.ts` `a11y` block — absent (b24ui dropped `@nuxt/a11y` in
  #120).
- `LinkBase.vue` `href?: string | null` — already applied in #117.

## Verification (pnpm 11.5.0, gate ON)
frozen install · dev:prepare · lint · typecheck · test (4994 passed, 6
skipped) · module build — all green.

Platform note: b24ui CI is `ubuntu-latest` only; dep bump.
Branch was updated with main (#158, #160) before PR.
