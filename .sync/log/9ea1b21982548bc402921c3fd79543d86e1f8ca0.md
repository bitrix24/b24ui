# port — nuxt/ui@9ea1b21982548bc402921c3fd79543d86e1f8ca0

**Upstream:** chore(deps): update pnpm to v11 (#6471)

**Decision:** port (adapted to b24ui's workspace/CI).

## Change
Migrate the package manager to **pnpm v11**:
- `package.json`: `packageManager` `pnpm@10.33.4` → **`pnpm@11.1.3`**; removed the
  `resolutions` block (v11 moves it to `pnpm-workspace.yaml` `overrides`).
- `pnpm-workspace.yaml`: migrated to the v11 schema —
  - `overrides:` ← b24ui's former `resolutions` (`@nuxt/kit`, `@bitrix24/b24ui-nuxt`,
    `nitropack`, `unplugin`, `c12`, `h3`, `unimport`);
  - `allowBuilds:` ← former `onlyBuiltDependencies` (better-sqlite3/puppeteer/sharp
    = `true`) + `ignoredBuiltDependencies` (@parcel/watcher, @tailwindcss/oxide,
    @vercel/speed-insights, esbuild, unrs-resolver, vue-demi = `false`);
  - added `linkWorkspacePackages: true`; kept `ignoreWorkspaceRootCheck: true`.
- `.github/workflows/ci.yml`: added `pnpm_config_strict_dep_builds: false` to the
  job `env` (v11 fails install on un-allowed build scripts otherwise).
- `docs/nuxt.config.ts` + `docs/package.json`: removed `@nuxt/a11y` (module,
  dependency, and b24ui's extra `a11y: { logIssues: false }` config block — the
  config key would be invalid once the module is gone).
- `pnpm-lock.yaml`: regenerated **under pnpm 11.1.3** + `pnpm dedupe`
  (single vite@7.3.5 / vue@3.5.38, `@nuxt/a11y` fully removed).

## b24ui adaptation notes
- Upstream touched `module.yml`; applied the CI env to b24ui's `ci.yml`
  (b24ui's workspace list / overrides / build-deps differ, so values are
  b24ui's, schema is upstream's).
- Did **not** add upstream's new `typescript` / `@nuxt/test-utils` overrides —
  kept the faithful migration of b24ui's existing resolutions only (verify is
  green without them).

## Validation
Toolchain switched to pnpm 11 locally via corepack. Verified **under pnpm
11.1.3**: frozen install · dev:prepare · typecheck · lint · `test run`
(4972 passed | 6 skipped) · module build — all green.

### Note: sandbox supply-chain policy
This remote env enforces a `minimumReleaseAge` supply-chain policy (not present
in the repo, ~/.npmrc, or `pnpm config`) that rejects 3 transitive entries
already in `main`'s lockfile (`nuxt-og-image@6.6.0`, `nuxtseo-shared@5.3.0`,
`postcss-selector-parser@7.1.4`, published < ~24h before the cutoff). Bypassed
**locally only** for install/verify via `--config.minimumReleaseAge=0`; GitHub
Actions CI has no such policy, so the committed lockfile installs normally
there. These versions are pre-existing (not introduced by this commit).
