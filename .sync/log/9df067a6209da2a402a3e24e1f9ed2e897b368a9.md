# Port: chore(deps): update tiptap to ^3.27.3 (#6719)

**Upstream:** `9df067a6209da2a402a3e24e1f9ed2e897b368a9` (nuxt/ui)
**Decision:** port — §2 manifest bump

## Upstream change
Bumps the whole `@tiptap/*` set from `^3.27.1` → `^3.27.3` across root/docs/
playground manifests + lockfile.

## b24ui port — §2 mirroring
Bumped every b24ui tiptap pin at `^3.27.1` → `^3.27.3`:
- **root `package.json`** — the 17-package tiptap set (core, extension-*,
  markdown, pm, starter-kit, suggestion, vue-3);
- **`docs` / `playgrounds/{nuxt,demo,vue}`** — `@tiptap/extension-emoji` +
  `@tiptap/extension-text-align` (fork-only extras, kept in lockstep so tiptap
  doesn't split across two minors);
- **`pnpm-workspace.yaml`** overrides — `@tiptap/pm: ^3.27.1` → `^3.27.3` (the
  override added during the `ad93121` bump to collapse a duplicate
  `prosemirror-view`).

Regenerated `pnpm-lock.yaml` with `CI=true pnpm install --lockfile-only`
(pnpm 11.10.0, lockfileVersion 9.0). Net −425 lines: bumping to 3.27.3 collapses
the previously-duplicated tiptap/prosemirror trees (incl. `prosemirror-view@1.41.8`
and `prosemirror-model@1.25.8`) into a single resolution — healthy dedup, not a
dropped dependency.

## Tests
Dep-only bump; no source/snapshot changes. Typecheck (`vue-tsc` + all `nuxt
typecheck` projects) green — confirms the prosemirror dedup didn't reintroduce the
TS2322 duplicate-`prosemirror-view` regression from the earlier tiptap bump. Suite
5485 passed / 6 skipped (unchanged).

## Verify (CI=true)
`dev:prepare` · `lint` · `typecheck` · `test` · `build` — all green.
