# port — nuxt/ui@9809cfe26b6d1e0685f16478d85eb4bb230e7c33

**Upstream:** chore(AGENTS): fix stale repl script and ThemeDefaults path

**Decision:** port — one of two hunks.

- **`dev:repl` → `repl`: already right here.** `AGENTS.md:36` reads `pnpm run
  repl`, and `package.json` has `repl` (no `dev:repl`).
  `test/utils/documented-scripts.spec.ts` guards every script `AGENTS.md`
  quotes, which is why this one never drifted.
- **`ThemeDefaults` path: ported.** Step 5 of the Component Creation Workflow
  sent the reader to `src/runtime/composables/useComponentProps.ts`, which
  contains no `ThemeDefaults` interface — `grep -c 'export interface
  ThemeDefaults'` returns 0 there and 1 in `src/runtime/types/theme.ts`.

## Same defect, two more places

This is one of three places carrying the stale path, and the other two are not
in this commit:

- **`cli/commands/make/component.mjs`** — the scaffolder writes the entry to the
  same wrong file, finds no interface, and returns silently. Fixed upstream by
  `4bfb47d7`, three commits later; ported in its own PR.
- **`.github/contributing/component-structure.md:478`** — states the same path,
  and adds that the CLI "auto-inserts the entry", which is false until
  `4bfb47d7`. Upstream still carries this line at `v4`'s tip. Corrected here
  alongside the `4bfb47d7` port, since the sentence only becomes true once the
  CLI is fixed.
