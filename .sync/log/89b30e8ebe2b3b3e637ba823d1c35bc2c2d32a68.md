# Port: feat(PinInput): add `separator` prop (#6392)

**Upstream:** `89b30e8ebe2b3b3e637ba823d1c35bc2c2d32a68` (nuxt/ui)
**Decision:** port

## Upstream change
Add a `separator` prop to PinInput to group inputs:
- `PinInput.vue`: `separator?: number | number[]` prop, a `PinInputSlots`
  interface with a `separator` slot, `defineSlots`, a `shouldInsertSeparator`
  helper, and a template rewrite (wrap `PinInputInput` in `<template v-for>`
  and emit a `<span data-slot="separator">` with a `#separator` slot fallback
  `•`).
- `theme/pin-input.ts`: add a `separator` slot class.
- spec cases + regenerated snapshots; docs section + example; playground page.

## b24ui adaptation
Same edits, b24ui-namespaced (`b24ui` prop/slots; theme is `() => ({...})`):
- `PinInput.vue`: import `VNode`; add the `separator` prop (jsdoc verbatim),
  `PinInputSlots`, `defineSlots<PinInputSlots>()`, `shouldInsertSeparator`, and
  the template rewrite using `b24ui.separator(...)`.
- `theme/pin-input.ts`: `separator: 'text-dimmed flex items-center justify-center'`
  (appended after the `base` array; `text-dimmed` is a valid b24ui token).
- spec: `with separator`, `with separator positions`, `with separator slot`
  cases; regenerated both snapshot projects (**+6**).
- example `PinInputSeparatorSlotExample.vue`: `UPinInput`→`B24PinInput`,
  `UIcon name="i-lucide-minus"` → `@bitrix24/b24icons-vue/actions/Minus40Icon`.
- playground pages: added a `:length="6" :separator="3"` row to **both**
  `playgrounds/nuxt` and `playgrounds/demo` pin-input pages (demo mirrored).
- docs `pin-input.md`: "Separator" section (two component-code blocks) +
  "Examples › With separator slot" + a `### Slots` (`:component-slots`) entry.
  Dropped upstream's `:badge{label="Soon"}` (the prop ships in b24ui).

## Verification (pnpm 11.5.2, gate ON)
dev:prepare · lint · typecheck (incl. docs) · test (5052 passed, 6 skipped —
+6 PinInput separator cases) · module build — all green.

Platform note: b24ui CI is `ubuntu-latest` only.
