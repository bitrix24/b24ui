# port — nuxt/ui@73daec1af120a52102be4ecd1b6dbe0245ed5291

**Upstream:** test(components): add highlight coverage across all form controls

**Decision:** port (test coverage; adapted to b24ui).

**Pairs with:** `a0deee4d` (#101) which added the `highlight` prop.

**What upstream did:** added `highlight` `renderEach` cases (primary + neutral, per
variant) to 15 component specs + regenerated snapshots.

**b24ui adaptation (not 1:1):**
- **No `neutral`** — b24ui uses the `air-*` color system; all upstream
  `color: 'neutral'` highlight cases are dropped.
- **No `Listbox`** in b24ui → skipped. 14 specs remain.
- b24ui specs don't enumerate colors per-variant; they use a representative
  single color case. So per the agreed shape, each spec gets **2 cases**:
  `['with highlight', …]` (default color) and
  `['with highlight air-primary-success', …]` (exercises the per-color ring via
  `--b24ui-border-color`). `air-primary-success` is valid in every one of the 14
  (each spec already tests it / inherits it).
- Per-file `props` shape preserved (`...props`, `defaultValue`, `CalendarDate` /
  `Time` values) to match each spec's existing cases.

**Specs touched (14):** Checkbox, CheckboxGroup, FileUpload, Input, InputDate,
InputMenu, InputNumber, InputTags, InputTime, RadioGroup, Select, SelectMenu,
Switch, Textarea.

**Validation:** `dev:prepare` · `eslint` · `vue-tsc --noEmit` · full
`vitest run -u` (regen new highlight snapshots) then full `vitest run` (per #74).
Snapshot growth is expected (new highlight render cases).
