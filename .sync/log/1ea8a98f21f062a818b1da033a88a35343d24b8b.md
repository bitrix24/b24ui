# Port: chore(deps): update reka-ui (#6626)

**Upstream:** `1ea8a98f21f062a818b1da033a88a35343d24b8b` (nuxt/ui)
**Decision:** port

## Upstream change
Bumps `reka-ui` `2.9.10` → `2.10.0` and wires the new reka props this version
exposes:
- Modal/Slideover: forward `unmountOnHide`; `DialogPortal` gains
  `:force-mount="(portalProps.disabled && props.unmountOnHide === false) || undefined"`
  so a non-portalled modal can keep its content mounted (SSR/SEO).
- Popover: forward `enableTouch` (hover mode).
- Select: forward `nullableValue`.
- App: omit `teleportTo` from `ConfigProviderProps` (now a reka prop b24ui
  doesn't surface).
- `useForwardProps`: reka 2.10.0 tightened `useEmitAsProps`' signature; cast the
  generic proxy emit at the call site (runtime-transparent).
- docs (modal/popover/slideover) + SSR tests for Modal/Slideover.

## b24ui adaptation
Direct 1:1 — b24ui matched the upstream pre-change structure for every file.
- `package.json`: `reka-ui` `2.9.10` → `2.10.0` (exact pin); lockfile regen
  under the gate (`lockfileVersion` `9.0` unchanged).
- `App.vue`, `useForwardProps.ts`, `Modal.vue`, `Slideover.vue`, `Popover.vue`,
  `Select.vue`: applied the same edits (Modal/Slideover keep b24ui's
  `useBlurOnOpen(...)` emit wrapper; `reactivePick` gained `unmountOnHide`).
- Tests: ported the Modal/Slideover SSR specs verbatim (b24ui component paths).
- Docs: added the "Unmount" section to `modal.md`/`slideover.md` and the
  `enable-touch` tip + `enableTouch: true` to `popover.md`, adapted to b24ui MDC
  (`B24Button`/`:b24-button`, `:placeholder`, `#body`); dropped the upstream
  `:badge{label="Soon"}` version marker.
- **Snapshots: 746 regenerated** — reka 2.10.0 adds `dir="ltr"` on popper/content
  wrappers and changes element serialization (same churn upstream regenerated);
  benign, no b24ui markup/logic change.

## Verification (pnpm 11.8.0, gate ON)
dev:prepare · lint · typecheck · test (**5117 passed**, 6 skipped — +16 SSR
cases across nuxt+vue) · module build — all green.

Note: PR #6626's sibling commits `54125f3f` (Popover enableTouch) and
`4deb61b9` (Modal/Slideover unmountOnHide) are empty marker commits — the
feature code lives entirely in this commit; they'll be recorded as no-op.
