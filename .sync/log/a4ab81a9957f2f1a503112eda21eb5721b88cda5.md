# No-op: docs: add calendar template to lists and ci

**Upstream:** `a4ab81a9957f2f1a503112eda21eb5721b88cda5` (nuxt/ui)
**Decision:** no-op — six files, none with a surface here that the change fits

## Upstream change

The follow-up to `4a3168fb`: having added the calendar template to the
templates page, this threads it through every other list that enumerates
templates.

## Why it does not apply, surface by surface

Checked each rather than concluding from the commit subject:

| upstream file | hunk | here |
| --- | --- | --- |
| `.github/workflows/module.yml` | adds `calendar` to a build matrix of template repos | **the file does not exist.** This fork's workflows are `ci`, `deploy`, `npm-publish`, `release-please`, `release-watchdog`; none builds template repositories |
| `README.md` | one bullet linking `nuxt-ui-templates/calendar` | our README has no template list at all |
| `docs/…/1.index.md` | rewrites the Nuxt/Vue template lists to linked demo URLs | that section does not exist here |
| `docs/…/2.installation/1.nuxt.md` | a `npm create nuxt@latest -- -t ui/calendar` block and a `::card` in the template card-group | neither exists. Our only `card-group` on that page is the package-manager install block (`sync="pm"`), which is a different thing that happens to share the component |
| `docs/nuxt.config.ts` | adds `vue calendar` to the LLM retrieval-keywords string | the string exists (ours says `bitrix24 ui` where upstream says `nuxt ui`), but the keyword would advertise a template that does not exist. Keywords are a promise about what this library ships |
| `skills/nuxt-ui/references/guidelines/conventions.md` | one line | §2: `skills/` is b24ui-authored, upstream skill prose is never replayed |

## What this is not

It is not "we are behind on templates". The two commits are upstream cataloguing
a repository it owns. The equivalent work here would be to author a b24ui
calendar template and then list it — a product decision, not a port.

## Verify (CI=true)

`lint` · `typecheck` · `test` · `build` — all green; nothing under `src/`,
`docs/` or `test/` changed, so this records the decision and the reasoning only.
