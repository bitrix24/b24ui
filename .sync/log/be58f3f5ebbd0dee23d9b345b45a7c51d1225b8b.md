# Port: docs(table): pin TanStack Table links to v8

**Upstream:** `be58f3f5ebbd0dee23d9b345b45a7c51d1225b8b` (nuxt/ui, #6864)
**Decision:** port — applies in full, minus a page we do not have

## Upstream change

Every `tanstack.com/table/latest` link becomes `tanstack.com/table/v8`, because
`latest` now resolves to a major the documented API no longer matches. Two
labels gain the version, and one link is corrected: the **column** pinning
section pointed at the **row** pinning API.

## b24ui port

Applies for the same reason it applies upstream, checked rather than assumed:
this fork depends on `@tanstack/vue-table ^8.21.3`, so every `latest` link
documents an API our Table does not implement.

18 links across two files — `docs/content/docs/2.components/table.md` (17) and
`skills/b24-ui-nuxt/references/recipes/data-tables.md` (1). Both labels widened
to name the major, as upstream did.

**We inherit the mislabelled link too.** Our line 523, in *With column pinning*,
read:

> the TanStack Table [Pinning APIs](…/features/row-pinning)

pointing at row pinning from the column-pinning section. Now
`[Column Pinning APIs](…/features/column-pinning)`, matching upstream's fix.
Worth noting because it is a content error rather than a version pin, and a
sweep of `latest` → `v8` alone would have carried it forward.

**Not applicable:** `docs/content/docs/1.getting-started/3.migration/2.v3.md`.
Our migration directory holds only `1.v2.md` — this fork has no v3 migration
page.

## Verify (CI=true)

`lint` · `test` · `docs:generate` — all green. Documentation only; no component,
theme or type is touched, so `typecheck` and `build` have nothing to see beyond
the usual.

Zero `tanstack.com/table/latest` remain; 18 `tanstack.com/table/v8`.
