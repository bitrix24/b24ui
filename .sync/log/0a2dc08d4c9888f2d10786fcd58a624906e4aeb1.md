# No-op: test(BlogPost): update snapshots

**Upstream:** `0a2dc08d4c9888f2d10786fcd58a624906e4aeb1` (nuxt/ui)
**Decision:** no-op — the component does not exist here

## Upstream change
Snapshot-only. It regenerates `BlogPost`'s two snapshot files to pick up the
`motion-reduce:transition-none` that `ce5b1df` added to `blog-post.ts`'s
`image` and `avatar` slots — a follow-up its own author left out of that commit.

## Why it does not apply
b24ui has no `BlogPost`: no `src/runtime/components/BlogPost.vue`, no
`src/theme/blog-post.ts`, no `test/components/BlogPost.spec.ts`. `blog-post.ts`
is already recorded as one of the five theme files absent here when `ce5b1df`
was ported.

Nothing to regenerate. The equivalent snapshot churn for the themes b24ui *does*
have was taken in the `ce5b1df` port itself — 212 snapshots — rather than
trailing behind in a separate commit.
