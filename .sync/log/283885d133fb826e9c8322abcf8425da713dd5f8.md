# No-op: docs(components): add missing `/docs` prefix to links

**Upstream:** `283885d133fb826e9c8322abcf8425da713dd5f8` (nuxt/ui)
**Decision:** no-op — the defect does not exist here

## Upstream change
Eight in-page links in `field-group.md` and `input.md` pointed at
`/components/<name>` instead of `/docs/components/<name>`, so they 404'd.

## Why it does not apply
b24ui's docs already carry the prefix — and the trailing slash the fork's
routing uses:

```
docs/content/docs/2.components/field-group.md:19
  Wrap multiple [Button](/docs/components/button/) within a FieldGroup …
```

Checked the class, not just the two files upstream touched. A repository-wide
grep for links missing the prefix (`](/components/`, `](/composables/`,
`](/getting-started/`) across `docs/content/` returns **nothing**, so there is
no stray to fix anywhere else either.

Nothing to port.
