# Port: chore(deps): update all non-major dependencies (#6497)

**Upstream:** `ca5accf3362eaf060e4d02e53daeab5272637880` (nuxt/ui, renovate)
**Decision:** port (selective — shared deps + pnpm + c12 override)

## Upstream change
Renovate batch of non-major (patch/minor) bumps across `package.json`,
`docs/package.json`, `playgrounds/nuxt/package.json`, the `c12` override in
`pnpm-workspace.yaml`, `packageManager` (pnpm 11.1.3 → 11.3.0), and a lockfile
regen.

## b24ui adaptation (matches prior renovate-batch precedent — port shared deps only)
Bumped only the deps b24ui pins at the **same** pre-bump range as upstream:

- **`package.json`**: `@tanstack/vue-virtual ^3.13.24→^3.13.25`,
  `unplugin-vue-components ^32.0.0→^32.1.0`, `ai ^6.0.185→^6.0.191`,
  `vitest ^4.1.6→^4.1.7`.
- **`docs/package.json`**: `@ai-sdk/mcp ^1.0.42→^1.0.43`,
  `@ai-sdk/vue ^3.0.185→^3.0.191`, `@nuxtjs/mcp-toolkit ^0.17.1→^0.17.2`,
  `@regle/core` & `@regle/rules ^1.25.2→^1.26.1`, `@takumi-rs/core ^1.3.0→^1.5.1`,
  `ai ^6.0.185→^6.0.191`, `valibot ^1.4.0→^1.4.1`,
  `vue-component-meta ^3.3.0→^3.3.1`.
- **`playgrounds/nuxt/package.json`**: `@ai-sdk/vue ^3.0.185→^3.0.191`,
  `ai ^6.0.185→^6.0.191`.
- **`pnpm-workspace.yaml`**: `c12` override `^3.3.2→^3.3.4`.
- **`packageManager`**: pnpm `11.1.3 → 11.3.0` (corepack confirmed it resolves
  in this env; lockfile regenerated with 11.3.0).

Skipped upstream bumps b24ui doesn't ship (`@ai-sdk/anthropic`,
`@ai-sdk/gateway`, `@iconify-json/lucide`) and upstream's `@nuxt/ui` version
bump (b24ui versions independently).

Lockfile regenerated **with the `minimumReleaseAge` gate ON** (no bypass) — all
resolved versions are aged (the batch is from 2026-05-26), so no pins were
needed. Lock churn: +82/-69.

## Verification (pnpm 11.3.0, gate ON)
frozen install · dev:prepare · lint · typecheck · test (4972 passed, 6 skipped)
· module build — all green.

Platform note: b24ui CI is `ubuntu-latest` only; the change is OS-independent
(dependency ranges + lockfile).
