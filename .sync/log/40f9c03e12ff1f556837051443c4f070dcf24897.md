# Port: chore(deps): update tiptap to ^3.26.0 (#6568)

**Upstream:** `40f9c03e12ff1f556837051443c4f070dcf24897` (nuxt/ui)
**Decision:** port

## Upstream change
Bump all `@tiptap/*` deps `^3.24.0` → `^3.26.0` across root `package.json`,
`docs/package.json`, `playgrounds/nuxt`, `playgrounds/vue` + lockfile regen.

## b24ui adaptation
Same bump, swept across **all** b24ui manifests that pin `@tiptap/*` at the
same range (playground-mirroring invariant):
- root `package.json` (17 entries), `docs/package.json` (2),
  `playgrounds/nuxt` (2), `playgrounds/vue` (2), **`playgrounds/demo` (2,
  mirrored)** — all `^3.24.0` → `^3.26.0`. `playgrounds/repl` pins none.
- lockfile regen under the active minimumReleaseAge gate (resolves to aged
  `3.26.1`; only specifier metadata changed since the resolved version was
  already in range).

## Verification (pnpm 11.5.0, gate ON)
frozen install · dev:prepare · lint · typecheck · test (5036 passed, 6
skipped) · module build — all green.

Platform note: b24ui CI is `ubuntu-latest` only; dep bump.
