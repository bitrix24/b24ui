# Port: chore(deps): update all non-major dependencies (#6592)

**Upstream:** `7e359508f820f19d9d1c63b98603f278e708c719` (nuxt/ui)
**Decision:** port
**Combined into:** the deps-batch PR with `39090327` + `c34ab927` (one branch,
one lockfile regen).

## Upstream change
Renovate non-major batch across `package.json`, `docs/package.json`,
`playgrounds/{nuxt,vue,repl}/package.json` + lockfile, plus a lint fix in
`docs/server/utils/extractSections.ts` (removed a useless `inHeader = false`).

## b24ui adaptation
Bumped every package b24ui **already pins**, to the same target version, and
mirrored across all b24ui manifests including the fork-only
`playgrounds/demo`. Packages b24ui does not have were skipped; b24ui-only pins
were left untouched.

Applied (b24ui-relevant subset of the batch):
- root `package.json`: `motion-v` ^2.3.0, `vue-component-type-helpers` ^3.3.5,
  `@nuxt/eslint-config` ^1.16.0, `ai` ^6.0.205, `eslint` ^10.5.0, `happy-dom`
  ^20.10.3, `vue` ^3.5.38, `vue-tsc` ^3.3.5, `tailwindcss` (dep) ^4.3.1,
  `@tailwindcss/postcss` ^4.3.1, `@tailwindcss/vite` ^4.3.1; `packageManager`
  `pnpm@11.6.0`.
- `docs/package.json`: `@ai-sdk/mcp` ^1.0.50, `@ai-sdk/vue` ^3.0.205,
  `@regle/core`/`@regle/rules` ^1.27.2, `@takumi-rs/core` ^1.8.5, `ai` ^6.0.205,
  `better-sqlite3` ^12.10.1, `motion-v` ^2.3.0, `nuxt-og-image` ^6.6.0,
  `nuxt-schema-org` ^6.2.1, `prettier` ^3.8.4, `tailwindcss` ^4.3.1,
  `vue-component-meta` ^3.3.5.
- `playgrounds/{nuxt,demo}`: `@ai-sdk/vue` ^3.0.205, `ai` ^6.0.205,
  `tailwindcss` ^4.3.1, `vue-tsc` ^3.3.5 (kept nuxt↔demo parity).
- `playgrounds/vue`: `tailwindcss` ^4.3.1, `vue` ^3.5.38, `vue-tsc` ^3.3.5.
- `playgrounds/repl`: `tailwindcss` ^4.3.1, `vue` ^3.5.38.

Skipped (not in b24ui): `@nuxtjs/color-mode` (see `39090327` log),
`@iconify-json/*` (b24ui uses b24icons), `@ai-sdk/anthropic`, `@ai-sdk/gateway`.
Left untouched: `peerDependencies.tailwindcss` `^4.0.0` (upstream bumped the
*dependency* range, not the peer), `@ai-sdk/deepseek` `^2.0.38` (b24ui-only).

### Knock-on lint fixes (eslint 10.4.1 → 10.5.0 / @nuxt/eslint-config 1.16.0)
The bump activated `no-useless-assignment`, which flagged two pre-existing spots:
- `docs/server/utils/extractSections.ts` — removed `inHeader = false` right
  before `break` (the **same** fix upstream made in this commit).
- `src/runtime/composables/useDevice.ts` — `let current: ScreenSize = 'xs'`
  is immediately overwritten by an exhaustive if/else (final `else`); dropped
  the dead initializer (`let current: ScreenSize`). b24ui-specific file, fixed
  analogously to keep `lint` green.

## Verification (pnpm 11.6.0, gate ON)
lockfile regen under the active minimumReleaseAge gate; dev:prepare · lint ·
typecheck · test (5090 passed, 6 skipped) · module build — all green.
lockfileVersion unchanged (`9.0`).
