# Port: chore(deps): update all non-major dependencies (#6529)

**Upstream:** `c19f1039e3970f968e01e1811ac797463fdf75db` (nuxt/ui, renovate)
**Decision:** port (selective — shared deps + pnpm)

## Upstream change
Renovate non-major batch across `package.json`, `docs/package.json`,
`playgrounds/nuxt/package.json`, `packageManager` (pnpm 11.3.0 → 11.5.0), and a
lockfile regen.

## b24ui adaptation (shared deps only, all manifests incl. demo)
Bumped the deps b24ui pins at the same pre-bump range:
- **`package.json`**: `@internationalized/date ^3.12.1→^3.12.2`,
  `@internationalized/number ^3.6.6→^3.6.7`,
  `@tanstack/vue-virtual ^3.13.25→^3.13.26`, `fuse.js ^7.3.0→^7.4.0`,
  `tinyglobby ^0.2.16→^0.2.17`, `ai ^6.0.191→^6.0.193`, `eslint ^10.4.0→^10.4.1`.
- **`docs/package.json`**: `ai ^6.0.191→^6.0.193`, `@ai-sdk/mcp ^1.0.43→^1.0.45`,
  `@ai-sdk/vue ^3.0.191→^3.0.193`, `@takumi-rs/core ^1.5.1→^1.6.0`,
  `vue-component-meta ^3.3.1→^3.3.3`.
- **`playgrounds/nuxt` & `playgrounds/demo`** (mirrored per the §2 invariant):
  `@internationalized/date ^3.12.1→^3.12.2`, `ai ^6.0.191→^6.0.193`,
  `@ai-sdk/vue ^3.0.191→^3.0.193`.
- **`packageManager`**: pnpm `11.3.0 → 11.5.0` (corepack confirmed it resolves;
  lockfile regenerated with 11.5.0).

Skipped: `@iconify-json/*` and `@ai-sdk/anthropic|gateway` (b24ui doesn't ship
them — uses `@bitrix24/b24icons-*`); `playgrounds/vue`'s `@internationalized/date`
is pinned older (`^3.9.0`) and diverges from upstream's range, so left as-is;
upstream's `@nuxt/ui` version bump n/a.

Lockfile regenerated **with the `minimumReleaseAge` gate ON** (no bypass) — all
resolved versions are aged (batch dated 2026-06-01). Lock churn: +62/-32.

## Verification (pnpm 11.5.0, gate ON)
frozen install · dev:prepare · lint · typecheck · test (4972 passed, 6 skipped)
· module build — all green.

Platform note: b24ui CI is `ubuntu-latest` only; OS-independent (dep ranges +
lockfile).
