# Port: chore(deps): update tiptap to ^3.26.1 (#6593)

**Upstream:** `c34ab9278c4000599619142465c93d2ff0b1a995` (nuxt/ui)
**Decision:** port
**Combined into:** the deps-batch PR with `39090327` + `7e359508` (one branch,
one lockfile regen).

## Upstream change
All `@tiptap/*` packages `^3.26.0` → `^3.26.1` across `package.json`,
`docs/package.json`, `playgrounds/{nuxt,vue}/package.json` + lockfile.

## b24ui adaptation
Bumped every `@tiptap/*` package b24ui pins to `^3.26.1`, mirrored across all
b24ui manifests:
- root `package.json` (17 pkgs): `@tiptap/core`, `extension-bubble-menu`,
  `extension-code`, `extension-collaboration`, `extension-drag-handle`,
  `extension-drag-handle-vue-3`, `extension-floating-menu`,
  `extension-horizontal-rule`, `extension-image`, `extension-mention`,
  `extension-node-range`, `extension-placeholder`, `markdown`, `pm`,
  `starter-kit`, `suggestion`, `vue-3`.
- `docs/package.json`, `playgrounds/{nuxt,demo,vue}`: `@tiptap/extension-emoji`,
  `@tiptap/extension-text-align` → `^3.26.1` (kept nuxt↔demo parity).

No `@tiptap/*` packages are missing relative to upstream; b24ui pins the same
set. Peer ranges untouched.

## Verification (pnpm 11.6.0, gate ON)
Part of the combined deps-batch regen + verify: dev:prepare · lint · typecheck ·
test (5090 passed, 6 skipped) · module build — all green.
