# Port: chore(deps): update all non-major dependencies (#6567)

**Upstream:** `9ad24cfdb49c6c203fa7a59e542fd19d7828113a` (nuxt/ui)
**Decision:** port (selective)

## Upstream change
Renovate non-major batch across root/docs/playgrounds + a `reproduire.yml`
action-pin bump + lockfile regen.

## b24ui adaptation
Selective — only deps b24ui shares at the same pre-bump range (and mirrored
into `playgrounds/demo` per the invariant):
- **root `package.json`**: `packageManager` pnpm@11.5.0→11.5.2,
  `@tanstack/vue-virtual` ^3.13.26→^3.13.28, `fuse.js` ^7.4.0→^7.4.2,
  `@vue/test-utils` ^2.4.10→^2.4.11, `ai` ^6.0.193→^6.0.197,
  `happy-dom` ^20.9.0→^20.10.2, `vitest` ^4.1.7→^4.1.8.
- **`docs/package.json`**: `@ai-sdk/mcp` ^1.0.45→^1.0.46,
  `@ai-sdk/vue` ^3.0.193→^3.0.197, `@comark/vue` ^0.3.1→^0.4.0,
  `@takumi-rs/core` ^1.6.0→^1.7.0, `ai` ^6.0.193→^6.0.197,
  `nuxt-og-image` ^6.5.1→^6.5.2, `nuxt-schema-org` ^6.0.4→^6.1.0,
  `shiki` ^4.1.0→^4.2.0.
- **`playgrounds/nuxt` + `playgrounds/demo`** (demo mirrored): `@ai-sdk/vue`
  ^3.0.193→^3.0.197, `@comark/vue` ^0.3.1→^0.4.0, `ai` ^6.0.193→^6.0.197.
- lockfile regen under the active minimumReleaseAge gate (frozen install green
  — all resolved versions aged; e.g. nuxt-schema-org → 6.2.1, ai → 6.0.201,
  happy-dom → 20.10.5).

Skipped (not in b24ui / n/a): `reproduire.yml` (no such workflow),
`@nuxt/icon` (b24ui has none), `@ai-sdk/gateway`, `@iconify-json/*`
(b24ui uses b24icons), `release-it`/`@release-it/*`. `playgrounds/vue` shares
none of these. The `nuxt-og-image: 6.5.3` override (aged pin) keeps winning
over the bumped docs spec.

## Verification (pnpm 11.5.2, gate ON)
frozen install · dev:prepare · lint · typecheck · test (5036 passed, 6
skipped) · module build — all green.

Platform note: b24ui CI is `ubuntu-latest` only; dep bump.
