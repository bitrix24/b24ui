# no-op — nuxt/ui@7224333b0d311ed0b5e94f71c260721ca813255b

**Upstream:** chore(release): v4.11.1

**Decision:** no-op — upstream's own release bookkeeping.

Fifth of ten commits in this run, taken with `5fd94e13` (§6 4b, contiguous).

Two files: `package.json` `4.11.0 → 4.11.1`, and 19 generated lines appended to
their `CHANGELOG.md`. This fork is versioned independently — `2.12.0` here,
driven by release-please from our own squashed subjects — so upstream's version
number and changelog have no counterpart to land in. Same decision as
`f62aa143`, `ada15803`, `2166ff77` and `d6341d55` before it.

## What the commit is good for

A release commit lists what went into the release, which makes it a free
cross-check on the ledger. All **14** commits named in the 4.11.1 notes are
present in `processed` with a decision:

| upstream | decision | |
| --- | --- | --- |
| `bbf1e14` | port | ChatMessages `color` |
| `90c45a6` | port | Checkbox/RadioGroup `flex-wrap` |
| `9bdb89b` | no-op | Checkbox/RadioGroup `border-default` |
| `e4cb042` | no-op | CommandPalette astral truncation |
| `a494a97` | port | Form nested `method` |
| `2d4782b` | port | InputNumber uncontrolled |
| `c4ee0ea` | port | Link prefetch under Nuxt 4.5 |
| `612ab1c` | port | Link `onNuxtReady` |
| `726e142` | port | NavigationMenu duplicate trigger |
| `fbb9e22` | n/a | ProseCodeTree |
| `e791d30` | port | Select/SelectMenu responsive size |
| `6caa6a9` | port | SelectMenu autofocus |
| `3d2de0c` | port | Slider single thumb |
| `2e8f533` | port | Table colspan |

Nothing unaccounted for. Worth doing rather than assuming: this is the one place
upstream states the set, and a gap here would mean a commit slipped past the
cursor.
