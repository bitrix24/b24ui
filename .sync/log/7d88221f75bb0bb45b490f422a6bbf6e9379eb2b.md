# Port: chore(deps): update tiptap to ^3.24.0 (#6532)

**Upstream:** `7d88221f75bb0bb45b490f422a6bbf6e9379eb2b` (nuxt/ui, renovate)
**Decision:** port

## Upstream change
Bumps every `@tiptap/*` package `^3.23.6 → ^3.24.0` across `package.json`,
`docs/package.json`, `playgrounds/nuxt/package.json`,
`playgrounds/vue/package.json` + lockfile regen.

## b24ui adaptation
b24ui pins the same `@tiptap/*` set; bumped all `^3.23.6 → ^3.24.0` across
**five** manifests (per the playground-mirroring invariant — also
`playgrounds/demo`):
- `package.json` — 17 packages (core, pm, starter-kit, vue-3, suggestion,
  markdown, extension-*).
- `docs` / `playgrounds/nuxt` / `playgrounds/vue` / `playgrounds/demo` —
  `@tiptap/extension-emoji` + `@tiptap/extension-text-align`.

Lockfile regenerated under pnpm 11.5.0 with the `minimumReleaseAge` gate ON;
the `^3.24.0` range resolves to the newest aged `@tiptap/*` (3.26.1).

## Verification (pnpm 11.5.0, gate ON)
frozen install · dev:prepare · lint · typecheck · test (4972 passed, 6 skipped)
· module build — all green.

Platform note: b24ui CI is `ubuntu-latest` only; OS-independent (dep ranges +
lockfile).
