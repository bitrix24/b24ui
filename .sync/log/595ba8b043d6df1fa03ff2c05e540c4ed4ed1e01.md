# Port: chore(deps): update tiptap to ^3.23.6 (#6498)

**Upstream:** `595ba8b043d6df1fa03ff2c05e540c4ed4ed1e01` (nuxt/ui, renovate)
**Decision:** port

## Upstream change
Bumps every `@tiptap/*` package `^3.22.4 → ^3.23.6` across `package.json`,
`docs/package.json`, `playgrounds/nuxt/package.json`,
`playgrounds/vue/package.json` + lockfile regen.

## b24ui adaptation
b24ui pins the same `@tiptap/*` set at `^3.22.4`; bumped all to `^3.23.6`
across **five** manifests (per the PORTING.md playground-mirroring invariant —
b24ui also has `playgrounds/demo`, which upstream lacks):
- `package.json` — 17 packages (core, pm, starter-kit, vue-3, suggestion,
  markdown, and the extension-* set).
- `docs/package.json` — `@tiptap/extension-emoji`, `@tiptap/extension-text-align`.
- `playgrounds/nuxt/package.json` — same two.
- `playgrounds/vue/package.json` — same two.
- `playgrounds/demo/package.json` — same two (b24ui-only).

`tiptap-extension-code-block-shiki` (docs, `^1.2.0`) is a separate third-party
package, not a `@tiptap/*` scope — upstream didn't touch it, left as-is.
Lockfile regenerated under pnpm 11.3.0 with the `minimumReleaseAge` gate ON
(3.23.6 dates from 2026-05-26 — aged).

## Verification (pnpm 11.3.0, gate ON)
frozen install · dev:prepare · lint · typecheck · test (4972 passed, 6 skipped)
· module build — all green.

Platform note: b24ui CI is `ubuntu-latest` only; OS-independent (dependency +
lockfile).
