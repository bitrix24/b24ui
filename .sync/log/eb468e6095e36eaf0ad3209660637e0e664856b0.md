# Port: fix(ChatMessage): add `wrap-break-word` to content slot

**Upstream:** `eb468e6095e36eaf0ad3209660637e0e664856b0` (nuxt/ui #6476)
**Decision:** port

## Upstream change
One-liner: add `wrap-break-word` to the `content` slot in `theme/chat-message.ts`
so long unbreakable strings/URLs wrap inside the message bubble instead of
overflowing. Plus regenerated ChatMessage/ChatMessages snapshots.

## b24ui adaptation
- `theme/chat-message.ts`: `content` first class string
  `'relative text-pretty *:first:mt-0 *:last:mb-0'` →
  `'relative text-pretty wrap-break-word *:first:mt-0 *:last:mb-0'`
  (b24ui keeps `content` as an array with extra b24 design-token classes; only
  the first base string changed).
- Regenerated ChatMessage/ChatMessages snapshots for both vitest projects
  (nuxt + vue) — the only delta is the added `wrap-break-word` class.

Follows directly on #122 (which had just removed `min-w-0` from `content`).

## Verification (pnpm 11.1.3, minimumReleaseAge gate ON)
frozen install · dev:prepare · lint · typecheck · test (4972 passed, 6 skipped)
· module build — all green.

Platform note: b24ui CI is `ubuntu-latest` only; the change is OS-independent
(a theme class string + snapshots).
