# skip — nuxt/ui@1022010a3d2046b638df641c12a94f6e5cff09d0

**Upstream:** docs(chat): use latest web search tool and v7 note

**Decision:** skip — the one hunk with a counterpart would make a false
statement here, and the rest have none.

| hunk | here |
| --- | --- |
| `chat.md`: *"The examples on this page target AI SDK v7."* | **false here.** The page's examples use `system:` and `.toUIMessageStreamResponse()` throughout — 23 matches in `chat.md`. |
| `chat.md`: `webSearch_20250305` → `webSearch_20260209` | ~~no counterpart~~ — **wrong, see Correction below.** The fork's `chat.md` does carry `anthropic.tools.webSearch_20250305({})` in its Web Search code group. |
| `playgrounds/nuxt/server/api/chat.post.ts` | no counterpart — the playground runs DeepSeek with `tools: {}`, and its prompt never mentions a `send_email` tool. |

## Why the v7 note is false

Upstream migrated its chat code to v7 idioms in nuxt/ui@dfbbfeb (#6636). This
fork **deferred** that migration by maintainer call (#238, ledger `dfbbfeb5`,
decision `skip`), recording *"b24ui stays on ai@6"*.

The package did not stay on 6 — the alignment in #425 moved the catalog to
`ai: ^7.0.107`. So the code runs on v7, through v7's deprecated shims. Checked
against the installed `ai@7.0.107` types:

- `system` — `@deprecated Use instructions instead`
- `toUIMessageStreamResponse()` — `@deprecated … This method will be removed in
  the next major release`

"Targets v7" would describe code that is written for v6 and survives on v7.

Filed as #635: the deferred migration, with the full list of affected files.
Once that lands, this commit's note becomes true and can be taken then.

## Correction — 2026-09-25

The web-search row above was wrong when written. `chat.md` here has the same
Anthropic web-search sample upstream has, `anthropic.tools.webSearch_20250305({})`.
The check that concluded otherwise was `grep … | head -10` over a pattern that
also matched `AI SDK` and `streamText`; the first ten matches were those lines
(7 through 129), and the `webSearch` line just after them was cut off. The skip itself
did not rest on that row — the v7 note was the reason — but the record said
something false and is corrected here rather than left.

**Both `chat.md` hunks were taken later**, in the PR that brought the Chat
docs' AI SDK samples to v7 idioms (#635): the "examples on this page target AI
SDK v7" sentence — true once the samples were migrated, as this entry said it
would be — and `webSearch_20250305` → `webSearch_20260209`, checked against
`@ai-sdk/anthropic@4.0.63`, which exports it. The playground hunk stays n/a:
the Nuxt playground has no chat route since the AI chat was removed (#640).
The decision field stays `skip` — it records what was decided when this commit
was processed — and this section records what happened after.
