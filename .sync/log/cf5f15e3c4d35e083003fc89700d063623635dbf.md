# No-op: docs(showcase): add codemo.dev

**Upstream:** `cf5f15e3c4d35e083003fc89700d063623635dbf` (nuxt/ui, #6663)
**Decision:** no-op — their showcase, not ours

## Upstream change

Two lines in `docs/content/showcase.yml` and a screenshot asset: Codemo joins
the list of sites built with Nuxt UI.

## b24ui

We have `docs/content/showcase.yml`, so the file exists — but it lists projects
built with **Bitrix24 UI**, and its own description says so. Adding a site built
on nuxt/ui would make the page assert something false.

This is content, not structure. Nothing to port and nothing to adapt; recorded
so the next porter does not re-derive it.
