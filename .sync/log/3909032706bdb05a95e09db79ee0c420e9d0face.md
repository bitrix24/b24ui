# Port: chore(deps): update dependency @nuxtjs/color-mode to v4 (#6050)

**Upstream:** `3909032706bdb05a95e09db79ee0c420e9d0face` (nuxt/ui)
**Decision:** no-op
**Combined into:** the deps-batch PR with `7e359508` + `c34ab927` (one branch
`sync/nuxt-deps-39090327-c34ab927`, one lockfile regen).

## Upstream change
`@nuxtjs/color-mode` `^3.5.2` → `^4.0.0` (root `package.json`) + lockfile, and
regenerated ContentSearch / DashboardSearch snapshots (color-mode v4 changes
nuxt/ui rendered output).

## b24ui decision — no-op
b24ui does **not** depend on `@nuxtjs/color-mode` in any manifest (root, docs,
playgrounds) — it has its own air-design color-mode handling. There is nothing
to bump and no snapshot churn (b24ui's ContentSearch/DashboardSearch don't
render color-mode output). N/A.

(The later non-major batch `7e359508` bumps the upstream pin again to `^4.0.1`,
also N/A here for the same reason.)

No change from this commit — bookkeeping only.
