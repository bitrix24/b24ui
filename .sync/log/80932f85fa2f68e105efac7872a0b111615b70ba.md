# n/a — nuxt/ui@80932f85fa2f68e105efac7872a0b111615b70ba

**Upstream:** chore(github): update nuxi config

**Decision:** n/a — the file does not exist in this fork, and should not.

`.github/nuxi.yml` configures a third-party triage bot
(`benjamincanac/nuxi`, later `tia`) installed on the `nuxt` organisation. Its
contents are nuxt's own: `maintainers: [benjamincanac]`, `package.name:
"@nuxt/ui"`, `securityPolicy:` pointing at `nuxt/.github`, `help:
https://ui.nuxt.com`, and a `nextMajor` branch this fork has no equivalent of.

This commit collapses `nextMajor` from an object to the string `v5`:

```diff
-nextMajor:
-  label: v5
-  branch: v5
-  package: https://pkg.pr.new/@nuxt/ui@{sha}
+nextMajor: v5
```

`.github/` here holds `ISSUE_TEMPLATE/`, `PULL_REQUEST_TEMPLATE.md`,
`contributing/`, `dependabot.yml`, `scripts/` and `workflows/` — no bot config
of any kind. Adopting one would mean adopting the bot, which is a decision about
this repository's triage process, not a port.
