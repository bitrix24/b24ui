# Port: chore(deps): update tiptap to ^3.30.2

**Upstream:** `dd4bc8e85365d45190716655430835906e5acef6` (nuxt/ui, #6876)
**Decision:** port — the whole `@tiptap/*` family, all on our pre-image
**Batched with** `a1776153` (#6875) into one PR; contiguous, §6 4b.

## Upstream change

The TipTap family from `^3.29.2` to `^3.30.2` across root, docs and both Nuxt
playgrounds.

## b24ui port

Every `@tiptap/*` entry this fork declares sat at `^3.29.2` — upstream's exact
pre-image — so all bump to `^3.30.2`:

- **root** (18 packages): `@tiptap/core`, `pm`, `starter-kit`, `vue-3`,
  `markdown`, `suggestion`, and the `extension-*` set
  (`bubble-menu`, `code`, `collaboration`, `drag-handle`, `drag-handle-vue-3`,
  `floating-menu`, `horizontal-rule`, `image`, `mention`, `node-range`,
  `placeholder`).
- **docs / playgrounds nuxt / playgrounds vue**: `@tiptap/extension-emoji`,
  `@tiptap/extension-text-align`.

Note on the parity file: `@tiptap/*` are tracked here because in the **root**
`package.json` both trees declare them as `dependencies` at the same version.
The `$note`'s remark about the family being "a peer `^3` upstream and a
dependency here" concerns the section mismatch that keeps *other* declarations
out of the snapshot; the root ones align and are compared.

## Verify

Editor is exercised by the suite (`test/components/Editor*.spec.ts`) and the
docs editor page prerenders under `docs:generate`. Shared gauntlet with #6875:
`dev:prepare` · `lint` · `typecheck` · `test` · `build` · `docs:generate`, all
green; `pnpm-workspace.yaml` unchanged.
